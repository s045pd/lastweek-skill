# lastweek

Agent skill for a seven-day community pulse.

- Contract: `skills/lastweek/SKILL.md`
- Engine: `python3 skills/lastweek/run.py TOPIC`
- Tests: `.venv/bin/pytest` after `python3 -m pip install -e ".[dev]"`

Do not answer a `/lastweek` request from host web search alone. Run the engine.
